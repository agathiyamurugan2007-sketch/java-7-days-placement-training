date = input.nextInt();
        month = input.nextInt();import java.util.Scanner;

class Palindrome {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int temp = n, rev = 0;

        while (n > 0) {
            int rem = n % 10;
            rev = rev * 10 + rem;
            n = n / 10;
        }

        if (temp == rev)
            System.out.println("Palindrome");
        else
            System.out.println("Not Palindrome");
    }
}
        year = input.nextInt();
        if(year>=1000 && year <= 9999)
        {
             if(month>=1 && month <=12)
                if((date>=1 && date <=31) && (month == 1 || month == 3 || month == 5 || month == 7 || month == 8 || month == 10 || month 12))
                  System.out.println("valid");

        }else if ((date>=1 && date <=30) && (month == 4 || month == 6 || month ==9 || month ==11))
               System.out.println("valid");
        else if(date )date = input.nextInt();




100/2=50
100/50=2   
this concept is called pairs of factor
100.,1
100/1=100
100/100=1
[1,100]
perfect square value
num --> abundant or perfect or deficient number


